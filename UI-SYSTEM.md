# nTrans UI 基础规则

本规则适用于 Popup、设置页、授权页和注入网页的 Shadow DOM 界面。参考图提供视觉方向，扩展界面以紧凑、高效和不遮挡网页为优先，不按展示稿等比放大。

## 视觉语言

- 采用 Warm Paper：暖白纸张底、近黑墨色文字、暖灰分隔线和克制的琥珀状态色。
- 主操作使用近黑纯色；琥珀色仅用于选中、进度、焦点和开关，不铺满大面积界面。
- 信息层级依靠字号、字重、留白和细边界，不使用卡片套卡片、渐变或厚重阴影。
- 面板圆角 8px，输入和按钮 6px；只允许浮动按钮、状态点、开关和徽标使用胶囊或圆形。
- 阴影只用于浮层和拖动按钮，普通设置区不使用阴影。

## 核心令牌

| 语义       | 浅色      | 深色      |
| ---------- | --------- | --------- |
| 页面背景   | `#f3efe7` | `#171613` |
| 内容面     | `#fbf8f2` | `#211f1b` |
| 次级内容面 | `#f5f0e7` | `#2b2822` |
| 正文       | `#211f1b` | `#f6f1e7` |
| 次要文字   | `#746f66` | `#b8b0a3` |
| 边界       | `#ded7ca` | `#474138` |
| 强调色     | `#b87912` | `#d39a37` |
| 焦点环     | `#c58a26` | `#d8a750` |

主操作使用浅色 `#23211e`、深色 `#f0e9de`，不使用渐变。成功、警告和错误继续使用语义色，不能改为强调色。

## 组件规则

- 浮动按钮：40px 暖白圆形视觉主体，保留 48px 点击区域；近黑正式翻译图标、琥珀活动点和真实进度环；拖动位置持久化，贴边收起后仍保留可恢复露出区域。
- 浮窗：桌面最大 336px，单层暖白面板；标题栏 44px，双 Tab 40px；每个 Tab 只放当前任务最常用的状态、语言、模式和主操作。
- Popup：固定 360px；状态列表、模式、语言和主操作按一屏优先排列。
- 设置页：全宽桌面工作区，左侧固定导航切换功能域；主区字段优先两列，窄屏降为一列并将导航变为顶部横向滚动。
- 选择器/输入框：设置页视觉高度 40px，浮窗紧凑规格 36px；标签包装区保留约 44px 的可点击范围，移动端控件回到 44px。
- 按钮/开关：直接操作目标保持约 44px；同一操作组只保留一个主按钮，标题栏图标按钮默认透明无框。
- 显示记忆：设置页使用独立 Tab 管理浮动控制的全局显示和会话隐藏恢复；翻译浮窗不堆叠隐藏/永久隐藏按钮。
- 状态：使用图标或形状、文字和颜色共同表达；错误提供可展开诊断，但不显示密钥、完整原文或完整响应。
- 字幕层：允许半透明圆角背景；“仅译文”在译文到达前保持空白，不短暂显示原文。

## 密度与间距

- 基础间距为 4px；常用间隔 6/8/12/16px。
- 浮窗内容内边距 8–10px；Popup 16px；设置区 16px，Tab 面板不额外套白底边框。
- 标题与说明间距 4px；字段标签与控件 6px；分组之间 12–16px。
- 不通过缩小触控目标制造密度；密度来自减少重复说明、减少嵌套容器和压缩无效留白。

## 交互与可访问性

- 所有浮层支持 Escape、点击外部关闭和焦点恢复。
- 键盘焦点使用 3px 琥珀色焦点环；不以 Hover 承载必需操作。
- 深浅色、`prefers-reduced-motion`、375px 窄屏和长文本都必须验收。
- 用户界面只描述能力和下一步，不暴露 OCR 引擎、模型名称、哈希或内部运行架构；技术和许可证信息放在开发文档与第三方声明中。
